---
context: phase
phase: 03-overage-pool-dashboard
task: verification-pending
status: in_progress
last_updated: 2026-05-12T22:15:00Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

_No blocking constraints discovered this session._

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Debug agent false positive | gsd-debugger identified 4→5 tuple early return as root cause, ran tests that passed, declared session complete — but the early return path was never hit (config.json exists). Root cause was actually hours_back window + date precision. | advisory | Before accepting a debug fix, verify the fixed code path is actually exercised by the real runtime conditions. |

<current_state>
Phase 03 (Overage Pool Dashboard) is functionally complete but has two bug fixes applied this session that need verification and committing. All changes are uncommitted working-tree edits. The app runs from `python monitor.py` (not `python cli/main.py`).

Two bugs were fixed this session:
1. Pool rows hidden because DataManager hours_back=192 gave only 7 sessions → threshold_state=calibrating → pool section gate failed. Fixed: orchestrator now passes hours_back=720.
2. Pool spend frozen at seed value because seed_date=tomorrow excluded all of today's new sessions from log scan. Fixed: auto_seed_from_anthropic now stores pool_spend_seed_datetime (precise UTC), _read_pool_config prefers it over the date-only fallback, _in_billing_period handles datetime cutoff.
</current_state>

<completed_work>

- Diagnosed pool rows not showing: threshold calibrating due to insufficient data window (hours_back=192, 7/10 sessions)
- Fixed hours_back: `monitoring/orchestrator.py` — DataManager now created with `hours_back=720`
- Diagnosed pool spend not updating: seed_date=tomorrow caused log_cutoff=tomorrow, excluding today's sessions
- Fixed seed datetime precision across 3 files:
  - `core/usage_fetcher.py` — stores `pool_spend_seed_datetime` (UTC ISO) + keeps `pool_spend_seed_date` (today, not tomorrow) for backward compat
  - `core/pool_state_manager.py` — `_read_pool_config` prefers `pool_spend_seed_datetime`, falls back to date; `_in_billing_period` handles `Union[date, datetime]` cutoff; `compute_pool_state` uses `seed_cutoff` variable
</completed_work>

<remaining_work>

- Verify fixes: restart `python monitor.py`, confirm pool rows appear and pool spend updates in real-time
- Run pytest to confirm no regressions (especially `tests/test_pool_state_manager.py`)
- Commit all uncommitted changes (see list below)
- Optionally: write a SUMMARY.md for phase 03
</remaining_work>

<decisions_made>

- hours_back=720 (30 days) chosen for DataManager — gives enough history for P90 calibration without being excessive
- datetime cutoff preferred over date-only — avoids the granularity problem where seed_date=today would include same-day sessions already captured in seed, and seed_date=tomorrow excluded all of today's new sessions
- pool_spend_seed_date kept in config.json (set to today, not tomorrow) for backward compatibility with any reader that doesn't understand the datetime field
- Entry point is `python monitor.py`, not `python cli/main.py` — Windows UTF-8 reconfiguration required for emoji rendering
</decisions_made>

<blockers>
None — all fixes applied, awaiting human verification.
</blockers>

## Required Reading (in order)
1. `core/pool_state_manager.py` — understand _read_pool_config return signature change (seed_date → seed_cutoff as Optional[datetime]) before any further edits
2. `core/usage_fetcher.py` — understand what auto_seed_from_anthropic now writes to config.json

## Infrastructure State
- All changes are uncommitted working-tree edits
- `~/.claude-monitor/config.json` has old `pool_spend_seed_date: "2026-05-13"` (tomorrow) — will be overwritten correctly on next `python monitor.py` startup when auto_seed_from_anthropic runs

## Uncommitted files
```
 M .planning/STATE.md
 M cli/bootstrap.py
 M cli/main.py
 M core/models.py
 M core/pool_state_manager.py
 M data/analyzer.py
 M data/reader.py
 M monitoring/orchestrator.py
 M pyproject.toml
?? core/usage_fetcher.py
?? .planning/debug/
?? .planning/phases/03-overage-pool-dashboard/03-PATTERNS.md
?? .planning/phases/03-overage-pool-dashboard/03-UAT.md
```

<context>
Phase 03 was already shipped in the last committed session. This session was post-ship debugging and fixing. Two real bugs were found and fixed. The previous debug session (gsd-debugger agent) found a valid latent bug (4→5 tuple early return) but misidentified it as the root cause — the actual root causes were hours_back window size and date-vs-datetime precision in the seed cutoff. Both are now fixed.
</context>

<next_action>
Start with: `python monitor.py` — confirm pool rows appear and pool spend value increments as tokens are used. Then run `pytest` and commit all uncommitted changes.
</next_action>
